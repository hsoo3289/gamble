<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team3.gamble.mybatis.board">

<resultMap id="boardResult" type="Board">

<result property="seq" column="SEQ"/>
<result property="subject" column="SUBJECT"/>
<result property="content" column="CONTENT"/>
<result property="writer_seq" column="WRITER_SEQ"/>
<result property="view_conunt" column="VIEW_COUNT"/>
<result property="like_conunt" column="LIKE_COUNT"/>
<result property="cdate" column="CDATE"/>
<result property="rdate" column="RDATE"/>
<result property="parent_seq" column="PARENT_SEQ"/>

</resultMap>

	<select id="mySelectAll" resultType="Board">
       select * from CONSULTING_BOARD order by SEQ;
    </select>
    
    
	
	<insert id="myInsert" parameterType="Board">
	insert into CONSULTING_BOARD(SEQ, SUBJECT, CONTENT, WRITER_SEQ, VIEW_COUNT,
						LIKE_COUNT, PARENT_SEQ)						
	 values(#{SEQ}, #{subject}, #{content}, #{writer_seq}, #{view_count}, #{like_count}, 
	   				       SYSDATE, SYSDATE, #{parent_seq})
	</insert>
	
	
	<update id="myUpdate" parameterType="Board">
	insert into CONSULTING_BOARD(SUBJECT, CONTENT)
		value(#{subject}, #{content})
	
	</update>
	
	
	<delete id="myDelete" parameterType="long">
		delete from CONSULTING_BOARD where SEQ= #{seq}
	
	</delete>
	
	
	
	
	
	
	
	</mapper>
	
	
